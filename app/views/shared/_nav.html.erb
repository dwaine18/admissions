<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Admissions Power</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"></li>
            <a class="nav-link active" aria-current="page" href="#"><%= link_to "Home", root_path %></a>
          
          
          <li class="nav-item"></li>
            <a class="nav-link active" aria-current="page" href="#"><%= link_to "About", about_path %></a>
          <!-- Contains heavy redirect logic to check if user has set up student or admin accounts and will redirect accordinly. -->
          <li class="nav-item"></li>
          <% if current_user%>
                <% if current_user.admin%>
                <a class="nav-link active" aria-current="page" href="#"></a><%= link_to "Admin Dashboard", adminDash_path %></a>
                <% else %>
                <%if  !Student.find_by(user_id: current_user.id)%>
                <%="/students/new"%>
                <%else%>
                <a class="nav-link active" aria-current="page" href="#"></a><%= link_to "Dashboard", dashboard_path %></a>
                <%end%>
                <%end%>
          <a class="nav-link active" aria-current="page" href="#"></a><%= link_to "Edit Account", edit_user_registration_path %></a>
          <a class="nav-link active" aria-current="page" href="#"></a><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></a>
            <% else %>
            <a class="nav-link active" aria-current="page" href="#"></a><%= link_to "Register Now", new_user_registration_path %></a>
            <a class="nav-link active" aria-current="page" href="#"></a><%= link_to "Sign In", new_user_session_path %></a>
            <%end%>
            </li>
                    </ul>
                    
                </div>
                </div>
            </nav>






